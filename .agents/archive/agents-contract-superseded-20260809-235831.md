# Sections removed from AGENTS.md when the managed block absorbed them

Removed from `C:\Users\dougl\Projects\168-audit\AGENTS.md`. Each heading below now lives inside the managed block, and the
text kept here is what the project carried outside it, which differed. Nothing here is
read by any tool; it exists so the removal is recoverable.

## Start and resume

1. Read this file.
2. Read `TASK.md` and the last 5ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Å“10 entries in `LOG.md`.
3. Read `BACKBURNER.md` for parked work.
4. Run `git status --short --branch` and `git worktree list --porcelain`.
5. Reconcile stale chat claims against files and Git before editing.
6. Run the repository state verifier when the local shared harness is available.
